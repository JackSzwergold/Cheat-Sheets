<!DOCTYPE html>
<html lang="en">

<head>
  <title>{% if meta.title %}{{ meta.title }} | {% endif %}{{ site_title }}</title>
  <meta http-equiv="x-ua-compatible" content="ie=edge">
  <meta http-equiv="content-type" content="text/html; charset=utf-8" />
  {% if meta.description %}
  <meta name="description" content="{{ meta.description|striptags }}" />
  {% endif %}
  {% if meta.robots %}
  <meta name="robots" content="{{ meta.robots }}" />
  {% endif %}
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/png" href="{{ theme_url }}/favicons/favicon.ico" />
  <link rel="stylesheet" type="text/css" href="{{ theme_url }}/css/bootstrap/bootstrap.css" />
  <link rel="stylesheet" type="text/css" href="{{ theme_url }}/fonts/font-awesome-4.7.0/css/font-awesome.min.css" />
  <link rel="stylesheet" type="text/css" href="{{ theme_url }}/css/style.css" />
  <script src="{{ theme_url }}/script/bootstrap/bootstrap.bundle.min.js" defer="defer"></script>
  <script src="{{ theme_url }}/script/jquery/jquery-3.5.1.min.js"></script>
  <script src="{{ theme_url }}/script/underscore/underscore-min.js"></script>
  <script src="{{ theme_url }}/script/javascript.js"></script>
</head>

<body>

  <div class="container-fluid">
    <div class="row">
      <div class="col px-5 bg-dark">

        <div class="container-fluid">
          <div class="row">

            <header class="col col-12 p-0 m-0 py-3 bg-dark">
              <span class="badge badge-pill badge-primary"><a href="{{ base_url }}" class="p-0 m-0 text-light font-weight-light text-decoration-none"><em class="fa fa-home mr-1"></em> Home</a></span>
              <h1 class="p-0 m-0 text-sinanova-black text-light">{% if meta.title %} {{ meta.title }} {% else %} {{ site_title }} {% endif %}</h1>
              <p class="p-0 m-0 text-sinanova-light text-light">By {{ meta.author }} ({{ meta.date|date("F j, Y") }})</p>
            </header>

            <main class="col col-12 p-0 m-0 p-3 bg-light">
              <article class="col col-12 p-0 m-0">
              <span class="text-sinanova-regular">
                {{ content }}
              </span>
            </article>
            </main>

            <footer class="col col-12 p-0 m-0 py-3 bg-dark">
              <p class="p-0 m-0 text-sinanova-regular text-light">
                <small>{{ meta.title }} (c) by {{ meta.author }}; written on {{ meta.date|date("F j, Y") }}.
                  This work is licensed under a Creative Commons Attribution-NonCommercial 4.0
                  International License (CC-BY-NC-4.0).</small>
              </p>
            </footer>

          </div>
        </div>

      </div>
    </div>
  </div>

</body>

</html>
